apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - configmap.yaml

commonLabels:
  component: pyth-pusher-ethereum
  app: pyth-pusher-ethereum

namespace: default
