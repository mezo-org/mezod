apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - configmap.yaml

commonLabels:
  component: pyth-pusher-mezo
  app: pyth-pusher-mezo

namespace: default
