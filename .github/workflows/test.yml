name: Tests
on:
  pull_request:
  push:
    branches:
      - main
      - release/**

jobs:
  test-unit-cover:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/setup-go@v5
        with:
          go-version: '1.20'
          check-latest: true
      - uses: actions/checkout@v3
      - uses: technote-space/get-diff-action@v6.1.2
        with:
          PATTERNS: |
            **/**.sol
            **/**.go
            go.mod
            go.sum
      - name: Test and Create Coverage Report
        run: |
          make test-unit-cover
        if: env.GIT_DIFF
      # TODO: Delete or uncomment, depending on whether we decide to use Codedev.
      # If we decide to use it, we need to set `CODECOV_TOKEN` as a secret.
      # - uses: codecov/codecov-action@v3
      #   with:
      #     file: ./coverage.txt
      #     fail_ci_if_error: true
      #   if: env.GIT_DIFF
