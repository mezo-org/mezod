# TODO for TET-1197: Remove version once we have a stable release.
npm_package_name=@mezo-org/euphrates-contracts@0.8.0-dev.1

bindings_environment := ethereum

# Contracts for which the bindings should be generated.
required_contracts := MezoBridge

include ../../../common/gen/Makefile
